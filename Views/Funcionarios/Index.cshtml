@model IEnumerable<MercadoIGL.Models.Funcionario>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome_Completo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Senha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cargo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome_Completo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Senha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cargo.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CPF">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CPF">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CPF">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
