@model IEnumerable<MercadoIGL.Models.Tipo>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoProduto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoProduto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Tipo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Tipo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Tipo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
